# Prompt line components
render_prompt_line() {
    echo -n "${PINK} ╰─▶ ${LAVENDER}"
}

render_right_prompt() {
    echo -n "${LAVENDER}󱨚 ${MAUVE}󱙺 ${PEACH}󱚢 ${YELLOW}󱚠 ${RED}󱚞 ${GREEN}󱜚 ${PINK}󱚦 ${RESET}"
}